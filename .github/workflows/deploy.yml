name: DevOps Pipeline
on:
   push:
      branches:
         - main

jobs:
  build:
  runs-on: ubuntu-latest
  steps:
  - uses: actions/checkout@v4

  - name: Build Docker Image
    run:  docker build -t devops-Project .

  - name: Test Container
   run: docker run -d -p 80:80:80
    devops-Project
